development:
    dialect: postgres
    datasource: user=choxi password=password dbname=dre_development sslmode=disable
    dir: migrations/
    table: migrations

production:
    dialect: postgres
    datasource: dre_production.db
    dir: migrations/
    table: migrations